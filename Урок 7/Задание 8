n = int(input())
if n >= 2:
    maximum = int(input())
    minimum = maximum
    for i in range(n - 1):
        number = int(input())
        if number > maximum:
            maximum = number
        else:
            number = minimum
print('Минимум равен', minimum)
print('Максимум равен', maximum)
